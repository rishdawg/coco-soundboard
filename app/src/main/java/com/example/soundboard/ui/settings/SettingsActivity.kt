package com.example.soundboard.ui.settings

import android.os.Bundle
import androidx.appcompat.app.AppCompatActivity
import com.example.soundboard.R

/**
 * Settings activity for the sound board (placeholder for future implementation)
 */
class SettingsActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        // This would be expanded in the future to include settings like:
        // - Grid size configuration
        // - Volume controls
        // - Haptic feedback settings
        // - Theme customization
        // - Sound recording options
    }
} 